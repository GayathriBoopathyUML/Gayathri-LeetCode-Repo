class Solution:
    def merge(self, nums1: List[int], m: int, nums2: List[int], n: int) -> None:
        """
        Do not return anything, modify nums1 in-place instead.
        """
        nums1[m:] = nums2  # Replace the zero placeholders with nums2 elements
        nums1.sort()  # Sort the merged list in-place